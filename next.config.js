/** @type {import('next').NextConfig} */
const nextConfig = {
  poweredByHeader: false,
  // Aquí también podrías poner las redirecciones WWW si no las haces por Vercel
}

module.exports = nextConfig